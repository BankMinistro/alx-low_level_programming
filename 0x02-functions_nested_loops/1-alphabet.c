#include <stdio.h>
/**
 *  main - functions nested loops tasks
 *
 * Return: Always (0)
 */
int main(void)
{
	char b;

	b = 'a';

	while (b <= 'z')
	{
		putchar(b);
		b++;
	}
	putchar('\n');
	return (0);
}
